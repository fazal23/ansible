RewriteEngine On

# map /wiki to w/index.php
RewriteRule ^/?wiki(/.*)?$ %{DOCUMENT_ROOT}/mediawiki/index.php [L]

# prevent users from accessing the wiki if they're just scanning IP addresses
RewriteCond %{HTTP_HOST}       !^.*{{ wiki.domain }}.* [NC]
RewriteRule .* - [F]
